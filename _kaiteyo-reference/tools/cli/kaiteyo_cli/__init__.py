"""Kaiteyo CLI — the Kaiteyo developer command center.

A single, cross-platform entry point for the repetitive repository operations
that developers perform every day: staged git workflows, Gradle task control,
WSL utilities, environment diagnostics, file/documentation browsing, and more.

Run with:  python -m kaiteyo_cli   (from tools/cli)
or via the launchers in tools/cli/bin/ (and the repo-root `kaiteyo` script).
"""

__version__ = "0.1.0"
