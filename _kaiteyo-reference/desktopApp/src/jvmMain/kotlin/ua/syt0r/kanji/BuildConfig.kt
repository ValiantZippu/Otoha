package ua.syt0r.kanji

/**
 * Desktop-side mirror of the generated core `BuildConfig` (core generates its
 * own as `internal`, which desktopApp cannot read). Values must be kept in
 * sync with the `buildConfig { }` block in core/build.gradle.kts.
 */
object BuildConfig {
    const val versionCode: Long = 2_210L
    const val versionName: String = "2.2.1"
    const val appDataAssetName: String = "kanji-dojo-data-base-v15.sql"
    const val appDataDatabaseVersion: Int = 15
}
